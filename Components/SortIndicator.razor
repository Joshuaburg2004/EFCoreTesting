@using BlazorWebAppEFCore.Grid
@inject IOnderdeelFilters Filters

@if (Column == Filters.SortColumn)
{
    <span>@SortSymbol</span>
}
else
{
    <span>&nbsp;</span>
}

@code {
    // Sets which OnderdeelFilterColumns this indicator is for.
    [Parameter]
    public OnderdeelFilterColumns Column { get; set; }

    // The symbol to render.
    private string SortSymbol => Filters.SortAscending ? "🔼" : "🔽";
}
